- /upload
- /upload-group endpoint for uploading multiple files as a group
- /delete/:code endpoint for deleting files and groups
- Group deletion endpoint (/delete-group/:code)
- Group download functionality (zip download for all files in group)
- Update file_groups table schema if not exists